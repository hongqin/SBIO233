tb = read.csv("mutation.csv")
tb
tb$Mu = tb$White / (tb$White + tb$Red)
tb
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution
tb
unique(tb$Group)
myReport = data.frame(groups);
groups = unique(tb$Group);
myReport = data.frame(groups);
myReport
tb = read.csv("mutation.csv")
tb$MuR = tb$White / (tb$White + tb$Red)
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution
groups = unique(tb$Group);
myReport = data.frame(groups);
myReport
tb = read.csv("mutation.csv")
tb$MuR = tb$White / (tb$White + tb$Red)
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution
groups = sort(unique(tb$Group));
myReport = data.frame(groups);
myReport
head(tb)
tb = read.csv("mutation.csv")
tb$MuR = tb$White / (tb$White + tb$Red)
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution
groups = sort(unique(tb$Group));
myReport = data.frame(groups);
for( i in 1:6) {
sub = tb[tb$Group=groups[i] );
myReport$MeanMu[i] = mean(sub$MuR);
myReport$StdMu[i] = sd(sub$MuR);
myReport$MeanDensity[i] = mean(sub$Density);
myReport$StdDensity[i] = sd(sub$Density);
}
tb = read.csv("mutation.csv")
tb$MuR = tb$White / (tb$White + tb$Red)
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution
groups = sort(unique(tb$Group));
myReport = data.frame(groups);
for( i in 1:6) {
sub = tb[tb$Group=groups[i], ];
myReport$MeanMu[i] = mean(sub$MuR);
myReport$StdMu[i] = sd(sub$MuR);
myReport$MeanDensity[i] = mean(sub$Density);
myReport$StdDensity[i] = sd(sub$Density);
}
i
tb = read.csv("mutation.csv")
tb$MuR = tb$White / (tb$White + tb$Red)
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution
groups = sort(unique(tb$Group));
myReport = data.frame(groups);
for( i in 1:6) {
 sub = tb[tb$Group=groups[i], ];
 #myReport$MeanMu[i] = mean(sub$MuR);
 #myReport$StdMu[i] = sd(sub$MuR);
 #myReport$MeanDensity[i] = mean(sub$Density);
 #myReport$StdDensity[i] = sd(sub$Density);
}
groups
i
i = 1:6
i
i = 1
sub = tb[tb$Group=groups[i], ];
tb
tb = read.csv("mutation.csv")
tb$MuR = tb$White / (tb$White + tb$Red)
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution
groups = sort(unique(tb$Group));
myReport = data.frame(groups);
for( i in 1:6) {
 sub = tb[tb$Group==groups[i], ];
 #myReport$MeanMu[i] = mean(sub$MuR);
 #myReport$StdMu[i] = sd(sub$MuR);
 #myReport$MeanDensity[i] = mean(sub$Density);
 #myReport$StdDensity[i] = sd(sub$Density);
}
myReport
tb = read.csv("mutation.csv")
tb$MuR = tb$White / (tb$White + tb$Red)
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution
groups = sort(unique(tb$Group));
myReport = data.frame(groups);
for( i in 1:6) {
 sub = tb[tb$Group==groups[i], ];
 myReport$MeanMu[i] = mean(sub$MuR);
 myReport$StdMu[i] = sd(sub$MuR);
 myReport$MeanDensity[i] = mean(sub$Density);
 myReport$StdDensity[i] = sd(sub$Density);
}
myReport
tb = read.csv("mutation.csv")
tb$MuR = tb$White / (tb$White + tb$Red)
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution
groups = sort(unique(tb$Group));
myReport = data.frame(groups);
for( i in 1:6) {
 sub = tb[tb$Group==groups[i], ];
 myReport$MeanMu[i] = mean(sub$MuR, na.rm=T);
 myReport$StdMu[i] = sd(sub$MuR, na.rm=T);
 myReport$MeanDensity[i] = mean(sub$Density);
 myReport$StdDensity[i] = sd(sub$Density);
}
myReport
tb = read.csv("mutation.csv")
tb$MuR = tb$White / (tb$White + tb$Red)
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution
groups = sort(unique(tb$Group));
myReport = data.frame(groups);
for( i in 1:6) {
 sub = tb[tb$Group==groups[i], ];
 myReport$MeanMu[i] = mean(sub$MuR, na.rm=T);
 myReport$StdMu[i] = sd(sub$MuR, na.rm=T);
 myReport$MeanDensity[i] = mean(sub$Density, na.rm=T);
 myReport$StdDensity[i] = sd(sub$Density, na.rm=T);
}
myReport
tb
tb = read.csv("mutation.csv")
tb$MuR = tb$White / (tb$White + tb$Red)
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution
groups = sort(unique(tb$Group));
myReport = data.frame(groups);
for( i in 1:6) {
 sub = tb[tb$Group==groups[i], ];
 myReport$MeanMu[i] = mean(sub$MuR, na.rm=T);
 myReport$StdMu[i] = sd(sub$MuR, na.rm=T);
 myReport$MeanDensity[i] = mean(sub$Density, na.rm=T);
 myReport$StdDensity[i] = sd(sub$Density, na.rm=T);
 myReport$OD[i] = sub$OD[1]
}
myReport
tb = read.csv("mutation.csv")
tb$MuR = tb$White / (tb$White + tb$Red)
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution
groups = sort(unique(tb$Group));
myReport = data.frame(groups);
for( i in 1:6) {
 sub = tb[tb$Group==groups[i], ];
 myReport$MeanMu[i] = mean(sub$MuR, na.rm=T);
 myReport$StdMu[i] = sd(sub$MuR, na.rm=T);
 myReport$MeanDensity[i] = mean(sub$Density, na.rm=T);
 myReport$StdDensity[i] = sd(sub$Density, na.rm=T);
 myReport$OD[i] = sub$OD[1]
}
myReport
pair(myReport)
tb = read.csv("mutation.csv")
tb$MuR = tb$White / (tb$White + tb$Red)
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution
groups = sort(unique(tb$Group));
myReport = data.frame(groups);
for( i in 1:6) {
 sub = tb[tb$Group==groups[i], ];
 myReport$MeanMu[i] = mean(sub$MuR, na.rm=T);
 myReport$StdMu[i] = sd(sub$MuR, na.rm=T);
 myReport$MeanDensity[i] = mean(sub$Density, na.rm=T);
 myReport$StdDensity[i] = sd(sub$Density, na.rm=T);
 myReport$OD[i] = sub$OD[1]
}
myReport
pairs(myReport)
tb = read.csv("mutation.csv")
tb$MuR = tb$White / (tb$White + tb$Red)
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution
groups = sort(unique(tb$Group));
myReport = data.frame(groups);
for( i in 1:6) {
 sub = tb[tb$Group==groups[i], ];
 myReport$MeanMu[i] = mean(sub$MuR, na.rm=T);
 myReport$StdMu[i] = sd(sub$MuR, na.rm=T);
 myReport$MeanDensity[i] = mean(sub$Density, na.rm=T);
 myReport$StdDensity[i] = sd(sub$Density, na.rm=T);
 myReport$OD[i] = sub$OD[1]
}
myReport
pairs(myReport)
plot( myReport$Density ~ myReport$OD );
myReport
plot( myReport$MeanDensity ~ myReport$OD );
text(myReport$OD, myReport$MeanDensity, myReport$groups);
plot( myReport$MeanDensity ~ myReport$OD, col='white' );
text(myReport$OD, myReport$MeanDensity, myReport$groups);
plot( myReport$MeanDensity ~ myReport$OD, col='white', main='Density ~ OD') );
text(myReport$OD, myReport$MeanDensity, myReport$groups);
plot( myReport$MeanDensity ~ myReport$OD, col='white', main='Density ~ OD' );
text(myReport$OD, myReport$MeanDensity, myReport$groups);
plot( myReport$MeanDensity ~ myReport$OD, col='white', main='Density ~ OD' );
text(myReport$OD, myReport$MeanDensity, myReport$groups);
m = lm(myReport$MeanDensity ~ myReport$OD);
abline(m, col="red");
summary(m)
tb = read.csv("mutation.csv")
tb$MuR = tb$White / (tb$White + tb$Red)
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution
groups = sort(unique(tb$Group));
myReport = data.frame(groups);
for( i in 1:6) {
 sub = tb[tb$Group==groups[i], ];
 myReport$MeanMu[i] = mean(sub$MuR, na.rm=T);
 myReport$StdMu[i] = sd(sub$MuR, na.rm=T);
 myReport$MeanDensity[i] = mean(sub$Density, na.rm=T)/1E6;
 myReport$StdDensity[i] = sd(sub$Density, na.rm=T)/1E6;
 myReport$OD[i] = sub$OD[1]
}
myReport
pairs(myReport)
plot( myReport$MeanDensity ~ myReport$OD, col='white', main='Density ~ OD' );
text(myReport$OD, myReport$MeanDensity, myReport$groups);
m = lm(myReport$MeanDensity ~ myReport$OD);
abline(m, col="red");
summary(m)
text( 1,2
)
tb = read.csv("mutation.csv")
tb$MuR = tb$White / (tb$White + tb$Red)
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution
groups = sort(unique(tb$Group));
myReport = data.frame(groups);
for( i in 1:6) {
 sub = tb[tb$Group==groups[i], ];
 myReport$MeanMu[i] = mean(sub$MuR, na.rm=T);
 myReport$StdMu[i] = sd(sub$MuR, na.rm=T);
 myReport$MeanDensity[i] = mean(sub$Density, na.rm=T)/1E6;
 myReport$StdDensity[i] = sd(sub$Density, na.rm=T)/1E6;
 myReport$OD[i] = sub$OD[1]
}
myReport
pairs(myReport)
plot( myReport$MeanDensity ~ myReport$OD, col='white', main='Density ~ OD', xlab="OD600nm", ylab="Density xE6/ml" );
text(myReport$OD, myReport$MeanDensity, myReport$groups);
m = lm(myReport$MeanDensity ~ myReport$OD);
abline(m, col="red");
summary(m)
text( 1,2
)
tb = read.csv("mutation.csv")
tb$MuR = tb$White / (tb$White + tb$Red)
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution
groups = sort(unique(tb$Group));
myReport = data.frame(groups);
for( i in 1:6) {
 sub = tb[tb$Group==groups[i], ];
 myReport$MeanMu[i] = mean(sub$MuR, na.rm=T);
 myReport$StdMu[i] = sd(sub$MuR, na.rm=T);
 myReport$MeanDensity[i] = mean(sub$Density, na.rm=T)/1E6;
 myReport$StdDensity[i] = sd(sub$Density, na.rm=T)/1E6;
 myReport$OD[i] = sub$OD[1]
}
myReport
pairs(myReport)
plot( myReport$MeanDensity ~ myReport$OD, col='white', main='Density ~ OD', xlab="OD600nm", ylab="Density xE6/ml" );
text(myReport$OD, myReport$MeanDensity, myReport$groups);
m = lm(myReport$MeanDensity ~ myReport$OD);
abline(m, col="red");
summary(m)
text( 1,1000, "R2=0.07, p=0.611");
myReport
# Do the mutation rate ~ OD plot
plot( myReport$MeanMu ~ myReport$OD, col='white', main='Density ~ OD', xlab="OD600nm", ylab="mutation rate" );
# Do the mutation rate ~ OD plot
plot( myReport$MeanMu ~ myReport$OD, col='white', main='Density ~ OD', xlab="OD600nm", ylab="mutation rate" );
text(myReport$OD, myReport$MeanMu, myReport$groups);
m2 = lm(myReport$MeaMu ~ myReport$OD);
abline(m2, col="red");
summary(m2)
# Do the mutation rate ~ OD plot
plot( myReport$MeanMu ~ myReport$OD, col='white', main='Density ~ OD', xlab="OD600nm", ylab="mutation rate" );
text(myReport$OD, myReport$MeanMu, myReport$groups);
m2 = lm(myReport$MeanMu ~ myReport$OD);
abline(m2, col="red");
summary(m2)
text( 1,1000, "R2=0.07, p=0.611");
tb = read.csv("mutation.csv")
tb$MuR = tb$White / (tb$White + tb$Red)
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution
groups = sort(unique(tb$Group));
myReport = data.frame(groups);
for( i in 1:6) {
 sub = tb[tb$Group==groups[i], ];
 myReport$MeanMu[i] = mean(sub$MuR, na.rm=T);
 myReport$StdMu[i] = sd(sub$MuR, na.rm=T);
 myReport$MeanDensity[i] = mean(sub$Density, na.rm=T)/1E6;
 myReport$StdDensity[i] = sd(sub$Density, na.rm=T)/1E6;
 myReport$OD[i] = sub$OD[1]
}
myReport
pairs(myReport)
#Do the density -OD plot
plot( myReport$MeanDensity ~ myReport$OD, col='white', main='Density ~ OD', xlab="OD600nm", ylab="Density xE6/ml" );
text(myReport$OD, myReport$MeanDensity, myReport$groups);
m = lm(myReport$MeanDensity ~ myReport$OD);
abline(m, col="red");
summary(m)
text( 1,1000, "R2=0.07, p=0.611");
# Do the mutation rate ~ OD plot
plot( myReport$MeanMu ~ myReport$OD, col='white', main='Density ~ OD', xlab="OD600nm", ylab="mutation rate" );
text(myReport$OD, myReport$MeanMu, myReport$groups);
m2 = lm(myReport$MeanMu ~ myReport$OD);
abline(m2, col="red");
summary(m2)
text( 1,0.25, "R2=0.35, p=0.21");
tb = read.csv("mutation.csv")
tb = read.csv("mutation.csv")
tb
tb$MuR = tb$White / (tb$White + tb$Red) #calculate mutation rate
tb
tb$MuR = tb$White / (tb$White + tb$Red) #calculate mutation rate
head(tb)
tb$MuR = tb$White / (tb$White + tb$Red) #calculate mutation rate
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution #calculate cell density
tb
tb$MuR = tb$White / (tb$White + tb$Red) #calculate mutation rate
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution #calculate cell density
groups = sort(unique(tb$Group)); #how many groups
myReport = data.frame(groups); #generate a class report
myReport
groups
tb = read.csv("mutation.csv") #read in the data
tb$MuR = tb$White / (tb$White + tb$Red) #calculate mutation rate
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution #calculate cell density
groups = sort(unique(tb$Group)); #how many groups
myReport = data.frame(groups); #generate a class report
#the follow codes generate report for the class
for( i in 1:6) {
 sub = tb[tb$Group==groups[i], ];
 myReport$MeanMu[i] = mean(sub$MuR, na.rm=T);
 myReport$StdMu[i] = sd(sub$MuR, na.rm=T);
 myReport$MeanDensity[i] = mean(sub$Density, na.rm=T)/1E6;
 myReport$StdDensity[i] = sd(sub$Density, na.rm=T)/1E6;
 myReport$OD[i] = sub$OD[1]
}
myReport
for( i in 1:6) {
 print(i)
 sub = tb[tb$Group==groups[i], ];
 myReport$MeanMu[i] = mean(sub$MuR, na.rm=T);
 myReport$StdMu[i] = sd(sub$MuR, na.rm=T);
 myReport$MeanDensity[i] = mean(sub$Density, na.rm=T)/1E6;
 myReport$StdDensity[i] = sd(sub$Density, na.rm=T)/1E6;
 myReport$OD[i] = sub$OD[1]
}
i =4
sub = tb[tb$Group==groups[i], ];
'
'
sub
 myReport$MeanMu[i] = mean(sub$MuR, na.rm=T);
 myReport$StdMu[i] = sd(sub$MuR, na.rm=T);
myReport
tb = read.csv("mutation.csv") #read in the data
tb$MuR = tb$White / (tb$White + tb$Red) #calculate mutation rate
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution #calculate cell density
groups = sort(unique(tb$Group)); #how many groups
myReport = data.frame(groups); #generate a class report
myReport
i =4
 sub = tb[tb$Group==groups[i], ];
 myReport$MeanMu[i] = mean(sub$MuR, na.rm=T);
 myReport$StdMu[i] = sd(sub$MuR, na.rm=T);
 myReport$StdMu[1] = sd(sub$MuR, na.rm=T);
myReport
tb
tb = read.csv("mutation.csv") #read in the data
tb$MuR = tb$White / (tb$White + tb$Red) #calculate mutation rate
tb$Density = (tb$White + tb$Red) * 10 * tb$Dilution #calculate cell density
groups = sort(unique(tb$Group)); #how many groups
myReport = data.frame(groups); #generate a class report
#the follow codes generate report for the class
for( i in 1:6) {
 print(i)
 sub = tb[tb$Group==groups[i], ];
 myReport$MeanMu[i] = mean(sub$MuR, na.rm=T);
 myReport$StdMu[i] = sd(sub$MuR, na.rm=T);
 myReport$MeanDensity[i] = mean(sub$Density, na.rm=T)/1E6;
 myReport$StdDensity[i] = sd(sub$Density, na.rm=T)/1E6;
 myReport$OD[i] = sub$OD[1]
}
myReport
#Do the density -OD plot. Q: Are the two cell density measures consistent? 
plot( myReport$MeanDensity ~ myReport$OD, col='white', main='Density ~ OD', xlab="OD600nm", ylab="Density xE6/ml" );
text(myReport$OD, myReport$MeanDensity, myReport$groups);
m = lm(myReport$MeanDensity ~ myReport$OD);
abline(m, col="red");
summary(m)
# Do the mutation rate ~ OD plot
plot( myReport$MeanMu ~ myReport$OD, col='white', main='Density ~ OD', xlab="OD600nm", ylab="mutation rate" );
text(myReport$OD, myReport$MeanMu, myReport$groups);
m2 = lm(myReport$MeanMu ~ myReport$OD);
abline(m2, col="red");
summary(m2)
text( 1,0.25, "R2=0.35, p=0.21");
